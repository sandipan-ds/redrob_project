# Redrob Candidate Ranker — pinned dependencies
# CPU-only, no network during ranking, Python 3.11+

# Core data & ML
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.13.0

# NLP / embeddings (offline, CPU)
sentence-transformers==3.0.1
transformers==4.41.2
torch==2.3.1          # CPU build — do NOT install torchvision/torchaudio
tokenizers==0.19.1

# Config & data
pyyaml==6.0.1
jsonschema==4.22.0

# Output / validation
pandas==2.2.2

# Dev / test only (not needed at ranking runtime)
pytest==8.2.2
pytest-cov==5.0.0
