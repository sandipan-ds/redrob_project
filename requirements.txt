# Redrob Candidate Ranker — pinned dependencies
# CPU-only, no network during ranking.
#
# REQUIRES Python 3.11.x (verified on 3.11.9). These pins have prebuilt wheels
# for 3.11 and install in seconds. They DO NOT build on Python 3.12+/3.14
# (no wheels -> source build -> scikit-learn/numpy build failure). The Stage-3
# Docker image MUST use python:3.11-slim so reproduction matches this env.
#   py -3.11 -m venv .venv
#   .\.venv\Scripts\python -m pip install -r requirements.txt

# Core data & ML
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.13.0

# NLP / embeddings (offline, CPU)
sentence-transformers==3.0.1
transformers==4.41.2
torch==2.3.1          # CPU build — do NOT install torchvision/torchaudio
tokenizers==0.19.1

# Config & data
pyyaml==6.0.1
jsonschema==4.22.0

# Output / validation
pandas==2.2.2

# Dev / test only (not needed at ranking runtime)
pytest==8.2.2
pytest-cov==5.0.0
